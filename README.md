# CryptoProject

install streamlit: py -m pip install streamlit

# HOW TO RUN THE SITE
- open the terminal in the folder where INTERFACE.py sits
- run the command *py -m streamlit run INTERFACE.py*
- the site should be running smoothly (:
